import React from 'react';
import { View, Text } from 'react-native';

import styles from './ScreensStyles';

const Service = ({ navigation }) => {
    return (
        <View style={styles.container}>
            <Text>ServicePage page</Text>
        </View>
    );
};

export default Service;
