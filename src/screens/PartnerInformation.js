import { View, Text } from 'react-native';
import styles from './ScreensStyles';

const PartnerInformation = ({ navigation }) => {
    return (
        <View style={styles.helpContainer}>
            <Text>Partner Information page</Text>
        </View>
    );
};

export default PartnerInformation;
